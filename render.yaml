services:
  - type: web
    name: ev-detector
    runtime: docker
    plan: free
    autoDeploy: true
    healthCheckPath: /api/v1/health
    envVars:
      - key: ENVIRONMENT
        value: production
      - key: LOG_LEVEL
        value: INFO
      - key: RATE_LIMIT_PER_MINUTE
        value: "60"
      - key: MAX_REQUEST_BYTES
        value: "134217728"
      - key: MAX_JSON_SAMPLES
        value: "100000"
      - key: MAX_CSV_SAMPLES
        value: "1000000"
      - key: CORS_ORIGINS
        value: "*"
      - key: ALLOWED_HOSTS
        value: "*"
